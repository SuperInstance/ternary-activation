[package]
description = "ternary-activation - SuperInstance ecosystem crate"
repository = "https://github.com/SuperInstance/ternary-activation"
name = "ternary-activation"
version = "0.1.0"
edition = "2024"

[dependencies]
ternary-types = { git = "https://github.com/SuperInstance/ternary-types.git", features = ["std"] }
